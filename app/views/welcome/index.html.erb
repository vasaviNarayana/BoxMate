<!DOCTYPE html>
<html>
<head>
  <title>Welcome to Boxmate</title>
  <%= csrf_meta_tags %>

  <style>
    body {
      font-family: Arial, sans-serif;
      text-align: center;
      margin: 0;
      padding: 0;
      background-color: #f5f5f5;
    }

    .container {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 80vh; /* Adjusted height for better visibility */
    }

    .welcome-container {
      background-color: #fff;
      border-radius: 5px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      max-width: 600px;
      padding: 20px;
      text-align: center; /* Align text to the center */
      margin-top: 20px; /* Reduced margin from the top */
    }

    h1 {
      color: #333;
    }

    p {
      color: #555;
    }

    .role-links {
      margin-top: 20px;
    }

    .role-link {
      display: inline-block;
      margin: 0 10px;
      padding: 10px 20px;
      background-color: #FFA500;
      color: #fff;
      text-decoration: none;
      border-radius: 3px;
      cursor: pointer;
    }

    .role-link:hover {
      background-color: #FF8C00;
    }

    .welcome-image {
        max-width: 100%;
        max-height: 150px; /* Adjusted height for better visibility */
        margin-bottom: 20px; /* Added margin to the image */
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="welcome-container">
      <img src="<%= asset_path('customer-image.png') %>" alt="Welcome Image" class="welcome-image">

      <h1>Welcome to Boxmate</h1>
      <p>Please choose your role:</p>

      <div class="role-links">
        <%= link_to "I'm a Chef", chefs_login_path, class: 'role-link' %>
        <%= link_to "I'm a Customer", customers_login_path, class: 'role-link' %>
      </div>
    </div>
  </div>
</body>
</html>
